jointstest.py是生成手部三维模型然后旋转该模型到对应角度然后剪影截图的脚本，现在本vue工程有部分功能是根据该输出的四元数和图片，在网页中围绕模型旋转摄像机视角达到与剪影图片的匹配，现在存在网页旋转视角后模型与剪影图片不匹配的问题，请帮我找出原因并提供解决方案。四元数文件在C:\Users\cibo\Desktop\my_left_mid_0113\data\data中的json文件里，本项目旋转摄像机部分在ThreeScene.vue里



现在我需要你检查项目中和根侧轮廓、尖侧轮廓有关的代码，第一，尖侧轮廓的D点的强制在左右两点之间的代码去除掉，同时固定D点位置，即只根据后端传来点位置数  
  据设置尖侧轮廓和根侧轮廓的D/A点，并使其在尖侧/根侧轮廓窗口中不可拖动；第二，移动AP1H点、H或移动DP1I点、I点控制的应该是模型右半边的点云，移动AP1G、G点  
  或DP1J、J点控制的是左半边点云，请检查相关代码，进行修复；第三，现在主三维窗口不能用右键改变相机视角，请禁用；第四，使用滑条调整模型旋转时，应该根据模  
  型自身的坐标轴进行旋转，如调节滚转应该是根据模型自身Z轴进行旋转，同时模型旋转后要根据模型当前的自身坐标轴进行下一步滑条旋转，同时上传到服务器或者保留  
  到本地保存的旋转数据应当是模型相对于世界轴的四元数